# Улицы и адреса Челябинска с координатами

Теги для поиска GitHub:  
Челябинск Челябинска Chelyabinsk координаты гео геокоординаты улицы улица адрес адреса street streets geo

LIVE TEST:
https://sovenov.github.io/Chelyabinsk_streets_geo_2024/

Адресная база Челябинска: улицы, дома, координаты и поле сортировки/приоритета. Данные актуализированы для моего проекта на момент 2024 года и опубликованы в открытый доступ в нескольких форматах: SQL, CSV, JSON, XML, YAML, ODS, PDF и других.

Изначально база использовалась в проекте `demo55.gkh-inform74.ru`. Проект не взлетел, поэтому сами данные я решил оставить доступными для тех, кому они могут пригодиться.

## Доступные файлы

Основным исходным дампом является [`streets8.sql`](./streets8.sql). Остальные файлы позволяют использовать или просматривать данные без предварительного импорта в MySQL/MariaDB.

| Файл | Формат | Для чего подходит |
| --- | --- | --- |
| [`streets8.sql`](./streets8.sql) | SQL | Импорт таблицы в MySQL/MariaDB, сохранение структуры, индексов и данных |
| [`streets8.csv`](./streets8.csv) | CSV | Импорт в таблицы, скрипты, аналитические инструменты и другие СУБД |
| [`streets8 (1).csv`](./streets8%20%281%29.csv) | CSV | Дополнительный CSV-файл, опубликованный в репозитории |
| [`streets8.json`](./streets8.json) | JSON | Веб-приложения, JavaScript/Python-скрипты, локальный поиск без СУБД |
| [`streets8.xml`](./streets8.xml) | XML | Обмен данными и интеграции, которым требуется XML |
| [`streets8.yml`](./streets8.yml) | YAML/YML | Чтение данных в инструментах и скриптах с поддержкой YAML |
| [`streets8.php`](./streets8.php) | PHP | Использование подготовленного представления данных в PHP-проектах |
| [`streets8.ods`](./streets8.ods) | ODS | Просмотр и фильтрация данных в LibreOffice Calc и совместимых редакторах |
| [`streets8.txt`](./streets8.txt) | TXT | Простой текстовый просмотр или обработка консольными утилитами |
| [`streets8.pdf`](./streets8.pdf) | PDF | Просмотр и передача данных в документном виде |
| [`streets8.doc`](./streets8.doc) | DOC | Просмотр в текстовых редакторах, поддерживающих формат DOC |
| [`streets8.odt`](./streets8.odt) | ODT | Просмотр в LibreOffice Writer и совместимых редакторах |
| [`streets8.tex`](./streets8.tex) | TeX | Подготовка документа через LaTeX |
| [`streets8.mediawiki`](./streets8.mediawiki) | MediaWiki | Публикация или перенос содержимого в MediaWiki |

> В репозитории присутствуют два CSV-файла: `streets8.csv` и `streets8 (1).csv`. Если второй файл является случайным дубликатом, лучше удалить его или переименовать перед публикацией, чтобы не создавать неоднозначность для пользователей.

## Структура данных

SQL-дамп [`streets8.sql`](./streets8.sql) содержит таблицу `streets8`:

| Поле | Тип | Описание |
| --- | --- | --- |
| `id` | `int(11)` | Уникальный идентификатор записи, `AUTO_INCREMENT` |
| `concat` | `varchar(50)` | Полный адрес в текстовом виде: улица/территория и номер дома |
| `geo` | `varchar(25)` | Координаты в формате `широта, долгота` |
| `priority` | `tinyint(1)` | Поле приоритета/сортировки |

В SQL-дампе:

- 41 992 адресные записи;
- кодировка `utf8mb4`;
- движок таблицы `InnoDB`;
- первичный ключ по `id`;
- уникальный индекс по `concat`.

Пример записи:

```sql
('1-й Аэропорт, 1-я Увельская улица, 1', '55.291244, 61.512876', 1, 1)
```

## Какой файл выбрать

| Задача | Рекомендуемый файл |
| --- | --- |
| Развернуть базу в MySQL/MariaDB | [`streets8.sql`](./streets8.sql) |
| Использовать адреса в локальной HTML/JavaScript-странице | [`streets8.json`](./streets8.json) или [`streets8.csv`](./streets8.csv) |
| Загружать данные в Python, BI-инструмент или другую СУБД | [`streets8.csv`](./streets8.csv) или [`streets8.json`](./streets8.json) |
| Открыть и отфильтровать адреса вручную | [`streets8.ods`](./streets8.ods) |
| Передать данные для просмотра без импорта | [`streets8.pdf`](./streets8.pdf) |

## Импорт в MySQL/MariaDB

Создайте базу данных и импортируйте основной SQL-дамп:

```bash
mysql -u USER -p -e "CREATE DATABASE chelyabinsk_streets CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;"
mysql -u USER -p chelyabinsk_streets < streets8.sql
```

Если база уже создана:

```bash
mysql -u USER -p DATABASE_NAME < streets8.sql
```

Дамп был выгружен из MariaDB `10.5.29`, но должен нормально импортироваться в современные версии MySQL и MariaDB.

## Примеры SQL-запросов

Найти адрес по части строки и отсортировать результаты сначала по приоритету, затем по адресу:

```sql
SELECT *
FROM streets8
WHERE `concat` LIKE '%Блюхера, 10%'
ORDER BY priority, `concat`;
```

Получить координаты конкретного адреса:

```sql
SELECT geo
FROM streets8
WHERE `concat` = 'улица Блюхера, 10';
```

Разделить координаты на широту и долготу средствами SQL:

```sql
SELECT
  `concat`,
  SUBSTRING_INDEX(geo, ',', 1) AS lat,
  TRIM(SUBSTRING_INDEX(geo, ',', -1)) AS lon
FROM streets8
WHERE `concat` = 'улица Блюхера, 10';
```

## Для чего можно использовать

- автодополнение адресов по Челябинску;
- геокодинг уже известных адресов;
- карты, справочники, внутренние CRM и ЖКХ-сервисы;
- проверка и нормализация пользовательского ввода;
- быстрый локальный справочник без обращения к внешним API.

## Важные замечания

Данные опубликованы как есть. Перед использованием в продакшене проверьте полноту и точность адресов для своего сценария, потому что городская адресная база может меняться.

Поле `geo` хранится строкой, а не отдельными числовыми колонками. Если планируются геопоиск, сортировка по расстоянию или пространственные индексы, лучше при импорте дополнительно разнести координаты в `DECIMAL`/`DOUBLE` или `POINT`.

Координаты полностью пригодны для использования на Яндекс Картах. В других картографических сервисах возможны небольшие неточности или смещения из-за различий в адресных базах, геокодировании и отображении объектов.

Файлы в форматах `DOC`, `ODT`, `PDF`, `TeX` и `MediaWiki` удобны прежде всего для просмотра или публикации. Для программной обработки данных практичнее использовать `SQL`, `CSV`, `JSON`, `XML` или `YML`.

## Лицензия

Репозиторий опубликован в открытый доступ. Если вам нужен формально закрепленный режим использования, добавьте файл `LICENSE` с подходящей открытой лицензией.

## Актуальность данных

Данные актуальны на момент 2024 года.
